package state;

import java.awt.Point;
import java.awt.event.MouseEvent;
import java.util.Iterator;

import page_elements.PageElement;
import view.PageView;

public class DeleteState extends State{
	
	PageView mediator;
	
	public DeleteState(PageView mediator) {
		this.mediator=mediator;
	}
	
	@Override
	public void mousePressed(MouseEvent e) {
		/* Point position = e.getPoint();
			int index;
			if(e.getButton() == MouseEvent.BUTTON1) {
				index = mediator.getModel().getDeviceAtPosition(position);
				if (index!=-1) {
				for (PageElement element : mediator.getModel().getSelected())
					mediator.getModel().getPageElements().remove(element);
		        //PageElement element = (PageElement) mediator.getModel().getDeviceAt(index);
		        //mediator.getModel().getPageElements().remove(element);
		        
				}else {
					System.out.println("Niste pogodili ni na jedan od selektovanih elemenata");
					mediator.getModel().getSelected().removeAll(mediator.getModel().getSelected());
				}
			}
			mediator.repaint();*/
	}
	
	

}
